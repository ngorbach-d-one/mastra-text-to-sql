# Dockerfile for running the MCP SQL server
FROM node:20-alpine

WORKDIR /app

# Install production dependencies
COPY package.json package-lock.json* ./
RUN npm ci --omit=dev

# Copy source code
COPY . .

# Default MCP server port
ENV PORT=3030
EXPOSE 3030

CMD ["npm", "run", "mcp"]
